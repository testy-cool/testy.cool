import VanillaMdx from "@/components/vanilla-mdx";
import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Stack",
  description: "Tools, services, and models I actually use - with opinions.",
  path: "/stack",
});

<VanillaMdx
  title="Stack"
  description="What I actually use, what I dropped, and what I think about all of it."
>

<StackPage />

</VanillaMdx>
