---
title: How to Remove Windows NUL File
description: That NUL file that appeared while vibe coding? Here's how to delete it.
date: 2026-01-06
author: testy.cool
tags: [windows, filesystem]
---

`NUL` is a reserved device name in Windows (like `CON`, `PRN`, `AUX`). These files typically get created when:
- Cross-platform tools (Git, WSL, Linux apps) create files without Windows name validation
- File redirections or scripts accidentally use reserved names
- Extracting archives from Unix/Linux systems

## When NUL Occurs

- Working with Git repositories from Linux/Mac
- Running cross-platform build tools
- File operations that don't respect Windows reserved names

## How to Remove on Windows

**PowerShell:**
```powershell
Remove-Item -Path "\\?\F:\full\path\to\nul" -Force
```

**Command Prompt:**
```cmd
del "\\?\F:\full\path\to\nul"
```

The `\\?\` prefix bypasses Windows name parsing, allowing you to access files with reserved names. Always use the full absolute path.

<FaqSchema items={[
  {
    question: "What is a NUL file in Windows?",
    answer: "NUL is a reserved device name in Windows, like CON, PRN, and AUX. These files get created when cross-platform tools such as Git, WSL, or Linux apps create files without Windows name validation, or when extracting archives from Unix/Linux systems."
  },
  {
    question: "How do I delete a NUL file on Windows?",
    answer: "Use PowerShell: Remove-Item -Path \"\\\\?\\F:\\full\\path\\to\\nul\" -Force, or Command Prompt: del \"\\\\?\\F:\\full\\path\\to\\nul\". The \\\\?\\ prefix bypasses Windows name parsing so you can access files with reserved names. Always use the full absolute path."
  }
]} />
